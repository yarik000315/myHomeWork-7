const categories = document.querySelectorAll("#categories>.item");
console.log(`в списку ${categories.length} категорії`);

const nameOfCategories = document.querySelectorAll(".item>h2");
const itemsOfCategories = document.querySelectorAll(".item>ul");

function getOurCountOfItems (types, items) {
    for (let i = 0; i < types.length; i++)
}
